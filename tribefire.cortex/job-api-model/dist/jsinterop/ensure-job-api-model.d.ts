/// <reference path="./job-api-model.d.ts" />

export declare namespace meta {
	const groupId: string;
	const artifactId: string;
	const version: string;
}

export import JobRequest = $T.com.braintribe.model.job.api.JobRequest;
export import JobResponse = $T.com.braintribe.model.job.api.JobResponse;
