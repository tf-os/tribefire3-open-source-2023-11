# hibernate-accesses-edr2cc-module

## bindHardwired()

Binds an enricher which sets the correct (backwards compatible) `Hibernate` mappings for the following system accesses:
* `auth`
* `user-sessions`
* `user-statistics`
* `transient-messaging-data`