# a concrete template for eclipse-workspaces with basic configuration
eclipseWorkspaceTemplate: "${config.sdk.conf}/templates/eclipse-workspace"
# the template for repository-configuration used for a new dev-env
# repoConfTemplate: "${config.sdk.conf}/templates/repository-configuration-devenv-mml.yaml"
repoConfTemplate: "${config.sdk.conf}/templates/repository-configuration-devenv-bt.yaml"
