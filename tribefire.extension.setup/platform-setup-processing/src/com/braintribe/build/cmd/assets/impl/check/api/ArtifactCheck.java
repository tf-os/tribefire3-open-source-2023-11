package com.braintribe.build.cmd.assets.impl.check.api;

/**
 * A {@link Check} interface for checks that are executed on artifacts.
 */
public interface ArtifactCheck extends Check<ArtifactCheckContext> {
	// empty block
}
