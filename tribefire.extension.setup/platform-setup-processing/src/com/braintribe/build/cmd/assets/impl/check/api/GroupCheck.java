package com.braintribe.build.cmd.assets.impl.check.api;

/**
 * A {@link Check} interface for checks that are executed on the whole artifact group.
 */
public interface GroupCheck extends Check<GroupCheckContext> {
	// empty block
}
